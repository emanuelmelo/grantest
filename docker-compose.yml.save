version: '3'
services:
   web:
        container_name: app
        build: .frontend
            context: .
            dockerfile: frontend/Dockerfile
        volumes:
            - '.:/app'
            - '/app/node_modules'
        ports:
            - '3001:3000'
        environment:
            - NODE_ENV=development
