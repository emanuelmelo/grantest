version: '3'
services:
   app:
        container_name: granfront
        build:
            context: .
            dockerfile: frontend/Dockerfile
        volumes:
            - '.:/app'
            - '/app/node_modules'
        ports:
            - '3001:3000'
        environment:
            - NODE_ENV=development
